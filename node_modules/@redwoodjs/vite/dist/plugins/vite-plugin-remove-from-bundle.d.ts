import type { PluginOption } from 'vite';
type ModulesToExclude = Array<{
    id: RegExp;
}>;
/**
 *
 * This is a vite plugin to remove modules from the bundle.
 *
 * Only applies on build, not on dev.
 *
 */
export default function removeFromBundle(modulesToExclude: ModulesToExclude): PluginOption;
export declare function excludeOnMatch(modulesToExclude: ModulesToExclude, id: string): {
    code: string;
} | null;
export {};
//# sourceMappingURL=vite-plugin-remove-from-bundle.d.ts.map