#!/usr/bin/env node
"use strict";

var _module = require("module");
const requireFromWebServer = (0, _module.createRequire)(require.resolve('@redwoodjs/web-server/package.json'));
const bins = requireFromWebServer('./package.json')['bin'];
requireFromWebServer(bins['rw-web-server']);